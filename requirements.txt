scrapling==0.4.7
pdfplumber==0.11.9
pandas>=2.0.0
openpyxl>=3.1.0
psycopg2-binary>=2.9.0
pytesseract>=0.3.13
pdf2image>=1.17.0
# requests + beautifulsoup4 are used only by scrapers/sebi_enforcement_orders.py
# (and any future scraper hitting an AJAX/XHR endpoint where the precise
# header/body shape matters). Project default remains Scrapling per PRD §10.
requests>=2.31.0
beautifulsoup4>=4.12.0
# FastAPI for the RBI ODI REST API (api/odi_api.py)
fastapi>=0.110
uvicorn>=0.29
# System deps (apt): tesseract-ocr (for pytesseract), poppler-utils (for pdf2image)
